# Spring Boot application layers example
Example of layered Spring Boot build

### Components
* H2 file DB for storage
* REST calls to create User records & view them

Layered model:
<img src="./src/main/resources/applayers.png" title="layers"/>